const os = require('os')

console.log(os.platform()); //os plaform(for windows win-X or name of operating system)
console.log(os.homedir()); //os home directory
console.log(os.freemem()); //os free memory
console.log(os.cpus()); // info about cpu